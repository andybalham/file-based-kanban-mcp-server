{
  "name": "@file-kanban/core",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "lint": "tsc -b --noEmit",
    "test": "npm run build && npm run test:built",
    "test:built": "node --test"
  },
  "dependencies": {
    "gray-matter": "^4.0.3"
  }
}
