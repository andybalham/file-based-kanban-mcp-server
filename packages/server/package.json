{
  "name": "@file-kanban/server",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "dist/main.js",
  "types": "dist/main.d.ts",
  "bin": {
    "file-kanban-mcp": "dist/stdio.js"
  },
  "scripts": {
    "build": "tsc -b",
    "lint": "tsc -b --noEmit",
    "test": "npm run build && npm run test:built",
    "test:built": "node --test"
  },
  "dependencies": {
    "@file-kanban/core": "0.0.0",
    "chokidar": "^3.6.0",
    "@modelcontextprotocol/sdk": "^1.17.3",
    "ws": "^8.21.0"
  },
  "devDependencies": {
    "@types/ws": "^8.18.1"
  }
}
