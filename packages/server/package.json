{
  "name": "@file-kanban/server",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "dist/main.js",
  "types": "dist/main.d.ts",
  "scripts": {
    "build": "tsc -b",
    "lint": "tsc -b --noEmit",
    "test": "node --test"
  },
  "dependencies": {
    "@file-kanban/core": "0.0.0"
  }
}
